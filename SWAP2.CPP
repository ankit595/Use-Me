#include<stdio.h>
#include <bits/stdc++.h>
int main()
{
      int a,b;
      printf("enter two numbers");
      scanf("%d%d",&a,&b);
	printf("a : %d , b : %d\n",a,b );
      swap(a, b);
      printf("a : %d  b : %d\n",a,b );
}
